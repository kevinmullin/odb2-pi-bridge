name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build:
    name: make ci (Docker)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: make ci
        run: make ci
